machine:
  node:
    version: iojs-v1.3.0

dependencies:
  pre:
    - npm install
    - npm install grunt-cli -g
    - bower install

deployment:
  production:
    branch: master
    commands:
      - grunt deploy
